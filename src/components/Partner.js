import React from "react";

const Partner = () => {
    return (
      <div style={styles.colour}>
        <p>Partner!</p>
      </div>
    );
};

const styles = {}

styles.colour = {
    width: '400px',
    height: '300px',
    background: 'green',
    position: 'absolute'
}

export default Partner;